<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{% block title %}{% endblock %}</title>

    <style>
        * {
            box-sizing: border-box;
        }

        .content {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100vw;
            height: calc(100vh - 50px);
        }

        form {
            width: 250px;
            display: flex;
            flex-wrap: wrap;
            padding: 1rem;
        }
        form * {
            width: 100%;
            margin: .25rem 0;
        }
    </style>
    {% block style %}{% endblock %}
</head>
<body style="margin: 0;">
    <header style="display: flex; justify-content: space-between; z-index: 100; ">
        <a href="/" style="padding: 1rem;">Home</a>
        <ul style="padding: 1rem; margin: 0; display: flex; justify-content: flex-end; list-style: none">
            {% if user %}
                <li style="margin: 0 1rem;">Welcome {{ user.login }}!</li>
                <li><a href="/logout">Logout</a></li>
            {% else %}
                <li style="margin: 0 1rem;"><a href="/signIn">Sign In</a></li>
                <li><a href="/signUp">Sign Up</a></li>
            {% endif %}
        </ul>
    </header>
    <div class="content">
        {% block content %}{% endblock %}
    </div>
</body>
</html>